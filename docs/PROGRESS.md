# 진행 상태 원장 (PROGRESS)

> **모든 세션은 이 파일을 읽는 것으로 시작하고, 이 파일을 갱신하는 것으로 끝난다.**

## 현재 상태 (2026-09-19)

- **목적:** 사내 PMS 재구축(v4). `C:\Projects\pms_mcp_v3`는 **읽기 전용 참조** —
  그 문서들은 역사이지 현행 스펙이 아니다.
- **진행:** 하네스·컨벤션·모델 구성 완료(verify 8단계). **B0 뼈대 통합 프로토타입 완료** —
  스냅샷 `001-b0-backbone`, 이관 로직 단언 35건 통과. 제품 코드 0줄 · 모듈 구성 미정(B5 이후).
- **다음 작업:** **B1 스펙** (`/next` → `/spec`, `spec/b1-backbone` 브랜치).
  B0에서 확정된 공통/영역별 구조·이관 내역 파생을 한 페이지로 옮긴다.
  이후 B2 → B3 → B4 → B5 순서로 단위마다 스펙 1장씩 — 프로토타입은 B0 스냅샷을 공유한다.
- **차단 요소:** 없음

## 결정 기록 (Decision Log)

| 날짜 | 결정 | 근거 |
|------|------|------|
| 2026-09-19 | v4를 새 리포로 시작, v3는 읽기 전용 참조 | v3는 기획을 처음부터 완벽히 잡으려다 구멍이 남았고, 하네스가 계획 중심이라 사이클당 코드량이 사람이 이해할 수 없는 양이 됐다 |
| 2026-09-19 | 기획 단위 = 기능 단위. 사이클 = 프로토타입 → 리뷰 → 1페이지 스펙 → 구현을 계속 반복 | 전면 기획은 검증되지 않은 결정을 대량으로 쌓는다. 단위를 쪼개면 틀린 결정의 사정거리가 한 단위로 제한된다 |
| 2026-09-19 | **전역 PRD 금지.** 스펙은 `docs/features/<NN-slug>.md` 한 장씩만 | v3 `PRD-pms.md` 150KB · `PROGRESS.md` 197KB — 한 문서가 커질수록 모순이 숨고 사람이 읽지 않는다 |
| 2026-09-19 | 기획은 **빌드 없는 순수 HTML 프로토타입**으로 먼저 확인 | v3 프로토타입은 React/Vite여서 확인하는 데 설치·빌드가 필요했고, 프로토타입 자체가 제품 코드처럼 커져 버려졌을 때 아까워졌다. 더블클릭으로 열리면 버리기 쉽다 |
| 2026-09-19 | 프로토타입 스냅샷은 **append-only**, 기획 단계마다 얼린다 | 기획이 왜 지금 모양이 됐는지는 결정 기록의 문장보다 그때의 화면이 더 정확하다. 권한(deny)과 verify 양쪽으로 잠갔다 |
| 2026-09-19 | **사이클당 diff 400줄 예산**(문서·스냅샷 제외), 초과 시 단위를 쪼갠다 | "한 사이클 코드량 과다"가 v4를 만든 고통이다. 지시문으로만 두면 지켜지지 않으니 verify가 숫자로 보고한다 |
| 2026-09-19 | 뼈대는 영업 → 솔루션 → 유지보수 이관 파이프라인. 공통 프로젝트 테이블 1 + 영역별 테이블 | 사용자 결정(2026-09-19). 이 줄기가 서기 전에는 가동률·알람 같은 기능을 붙이지 않는다 |
| 2026-09-19 | **제품은 PMS. MCP/AI 챗은 기능 단위 하나(F9)로 내린다** | v3는 이름부터 `pms_mcp` — MCP가 제품이어서 호스트 앱 분리·도구 카탈로그·2인 트랙 분리가 PMS 설계를 앞질렀고, PMS가 MCP 모양으로 휘었다. v4는 단일 트랙이고, F9 사이클 전에는 MCP를 위한 구조를 예약하지 않는다 |
| ~~2026-09-19~~ | ~~기술 스택 미정 — 뼈대 확인 후 결정~~ → **철회(같은 날)** | 아래 줄로 대체 |
| 2026-09-19 | **기술 스택 확정**: Java · Spring Boot · Modulith · JPA · PostgreSQL · Gradle · React/Vite/TS (v3 승계). 세부 버전은 앱을 세우는 사이클에서 고정 | 사용자 결정. v3 컨벤션을 지금 가져오려면 스택이 확정돼 있어야 한다 — 보류한 채로 컨벤션만 들이면 문서가 결정을 몰래 내린 셈이 된다. 팀이 이미 그 스택으로 돌고 있고 컨벤션은 실측 비용을 치른 자산이다 |
| 2026-09-19 | v3 `docs/conventions` 3종을 **개정해서** 승계 (원문 그대로 아님) | 대조 결과 그대로 쓸 수 없었다 — 2인 협업 전제 13곳(타인 리뷰 머지 조건·트랙 분업), v4에 없는 경로 참조 12곳(PRD-pms·PRD-host·host/CLAUDE.md·CI 워크플로·PROGRESS-host/pms), MCP 조항 14곳(불변식 6번과 충돌), java-spring §5의 v3 모듈 목록(v4의 세 영역 절단면과 불일치). 원문 그대로였다면 스택 보류 결정과 불변식 6번이 문서 안에서 조용히 뒤집혔다 |
| 2026-09-19 | MCP·챗 위젯 규칙은 **삭제하지 않고 각 컨벤션의 부록으로 유보** | v3가 실제로 값을 치른 규칙(도구 출력이 LLM에 직행 · 어댑터는 서비스만 호출 · 인젝션 방어)이라 버리면 F9에서 다시 산다. 본문에 두면 지금의 아키텍처를 휘게 하므로, F9 사이클이 부록을 본문으로 올리는 방식으로 되살린다 |
| 2026-09-19 | 모듈 구성은 B5 이후에 정한다. 모듈은 필요해진 사이클이 만든다(선제 스캐폴딩 금지) | v3는 `resource`·`notification`을 로직보다 먼저 세웠고 그 예외가 관행이 됐다. v4의 절단면은 세 영역 + 공통 project라 v3 목록을 그대로 쓸 수 없다 |
| 2026-09-19 | git 워크플로는 단일 트랙으로 다시 씀. 머지 조건 = verify 초록 + reviewer APPROVE + 사람의 눈 | v3 조건(CI 초록 + 타인 리뷰 1건)은 v4에 CI도 리뷰어도 없어 첫날부터 못 지키는 규칙이 된다. 못 지킬 규칙을 적어두면 규칙 전체의 구속력이 내려간다 |
| 2026-09-19 | verify에 **문서 참조 검사** 추가 | 컨벤션 이식에서 실측한 고통 — 타 리포에서 옮긴 문서의 깨진 경로 12곳은 아무도 모르게 남는다. 프로토타입 링크만 검사하던 구멍을 막았다 |
| 2026-09-19 | **영역별 행은 프로젝트당 1개(1:1)**. 이관해도 행을 지우지 않고 상태만 바꿔 남긴다 | 사용자 결정. 제가 "1:N이라야 이력이 남는다"고 물었던 것이 틀렸다 — 이력은 *행 개수*가 아니라 *지우지 않는가*의 문제였다. 영역 테이블이 셋이고 각 행이 남으므로 1:1로도 이관 내역이 온전하다 |
| 2026-09-19 | **이관 내역은 저장하지 않는다.** 영역별 행의 `openedAt`/`closedAt`에서 파생한다 | 이관 로그 테이블을 따로 두면 그 로그와 실제 행이 어긋나는 날이 온다(원본 이중화 금지). 프로토타입은 타임라인 각 줄 아래에 출처 행·컬럼을 찍어 이 사실을 화면이 스스로 증명하게 했다 |
| 2026-09-19 | **목록을 가르는 기준은 `phase` 컬럼이 아니라 "그 영역의 행을 가지고 있는가"** | 사용자 결정(이관해도 기존 영역에서 지워지지 않아야 한다). 저장 컬럼 하나가 목록 셋을 가르면 그 컬럼이 곧 두 번째 원본이 된다. 현재 영역도 같은 방식으로 파생된다 |
| 2026-09-19 | **유지보수의 원천은 계약이다**(프로젝트가 아니다). 계약:사이트 1:N, 이슈는 사이트에 붙는다 | 사용자가 준 내비 구조가 `유지보수 › 계약`이었다. v3 실측과도 일치(가온아이 1계약 45사이트) |
| 2026-09-19 | **화면은 영역마다 갈린다**(한 화면 탭 아님). 내비 = 홈 / 영업·솔루션·유지보수 / 인력·조직 · 로그 | 사용자 결정. 권한별 표시(F5)가 붙을 자리도 이 구조가 만든다 — 탭이면 항목 단위로 가릴 수 없다 |
| 2026-09-19 | **진행률 100%는 유지보수 이관의 전제조건**이다. 100% 자체는 상태를 바꾸지 않고, 완료는 이관이 만든다 | 사용자 지시(100%인 건에만 이관 버튼). v3는 진행률 쓰기가 상태 전이 권한을 우회해 자동 전이를 폐지했는데, 100%가 *버튼을 열어 줄 뿐*이면 같은 구멍이 생기지 않는다 |
| 2026-09-19 | 영역 안의 세부 상태는 **영역별**이다 — 영업 `리드·제안·견적·수주` / 솔루션 `진행 전·진행·검수·완료` / 계약 `신규·유지·종료` | 프로토타입에서 서로 다른 어휘를 넣어 보고 어색하지 않음을 확인. 공통 상태값을 두면 세 영역이 한 열거형을 공유하게 되고, 한 영역의 변경이 나머지를 흔든다 |
| 2026-09-19 | **diff 예산에서 `prototype/` 제외** (문서·스냅샷에 이어) | 예산의 목적은 *개발자가 검수할 코드량* 제한이다. 프로토타입은 버릴 물건이고, 이관 파이프라인은 끝에서 끝까지 눌러봐야 검증된다. B0 프로토타입이 1,270줄이었고 그게 옳았다 — 대신 **스펙·구현은 단위별로** 쪼갠다(v3가 무너진 지점은 거기다) |
| 2026-09-19 | 프로토타입은 **단일 해시 라우팅 페이지**도 허용한다(화면당 1파일이 기본) | `file://`에서는 문서 간 상태 공유가 안 된다. 이관 버튼이 자기가 한 일을 잊어버리면 검증하는 것이 없다 |
| 2026-09-19 | **B0 = 프로토타입 전용 단위** 신설. 한 프로토타입이 여러 단위를 덮되 스펙·구현은 단위마다 따로 | ROADMAP 표의 프로토 칸이 여러 행에서 동시에 채워지는 현실을 표가 정직하게 드러내게 했다 |
| 2026-09-19 | 기본 모델 **Opus 5**를 `.claude/settings.json`에 고정 | v4가 막으려는 건 처리량이 아니라 판단이다 — 단위를 어디서 자를지, 프로토타입이 실제로 답한 게 뭔지, 스펙에 **적지 않을** 것이 뭔지. 리포에 고정하면 다른 머신에서도 같은 모델로 돈다(대신 그 프로젝트의 `/model` 선택을 덮어쓴다) |
| 2026-09-19 | reviewer는 **sonnet 단일**. 저자(opus)와 다른 모델을 쓴다 | 역할 분리의 값어치는 권한 분리만이 아니라 **맹점 분리**다. 같은 모델이 쓰고 같은 모델이 판정하면 같은 것을 놓친다 |
| 2026-09-19 | haiku를 **둘째 판정자로 쓰지 않는다** | ① 판정 부하가 무겁다(불변식 12건 + 컨벤션 31.5KB + 사이클 규율) ② 놓친 판정자는 없는 판정자보다 나쁘다 — APPROVE가 나오면 사람이 diff를 덜 본다(harness_practice 6a 실측: 기계 검증은 조작된 테스트에 장님이었다) ③ **haiku가 확실히 잡을 항목은 전부 기계 검사가 가능하고, 기계가 더 싸고 틀리지 않는다** |
| 2026-09-19 | reviewer의 BLOCKER 8건 중 **기계 항목 6건을 verify.sh로 이관** | 0단계 원칙(확률적 지시 vs 결정론적 구조)의 적용. **브랜치 이름을 의도 선언으로 삼아** 판단 없이 검사한다 — `spec/<slug>`면 스냅샷이 있어야 하고 `feat/<slug>`면 스펙이 있어야 한다. reviewer에는 판단이 필요한 5건만 남겼다 |
| 2026-09-19 | **검사 부품도 실패 실험을 통과해야 추가한다** | 이 세션에서 실측: `check_script_eol`의 첫 구현이 awk를 썼는데 Git Bash의 awk는 CR을 텍스트 모드로 삼켜 **CRLF 파일에 PASS를 줬다**. 초록불이 거짓일 수 있다는 것을 검사 자체에서 확인. tr로 바꾸고 재실험해 통과 |
| 2026-09-19 | 지시문(CLAUDE.md·커맨드·에이전트·스크립트) 영어 / 기록물(PROGRESS·ROADMAP·BACKBONE·features) 한국어 / 코드 주석 한국어 | v3 결정 승계 — LLM 지시문은 영어가 더 안정적, 기록물은 사람이 읽는 문서 |
| 2026-09-19 | ralph 자율 루프·CI·스킬은 아직 넣지 않는다 | 고통 확인 전 부품 추가 금지. 특히 자율 루프는 "사람이 사이클을 검수한다"는 v4의 전제와 정면으로 어긋난다 |

## 세션 로그

### 형식 (복사해서 사용)

```
### YYYY-MM-DD — <단위>(<단계>) <작업 요약>
- 완료: <한 것 + 검증 결과 + diff 줄수>
- 미해결: <다음 사이클로 넘기는 것>
- 다음 작업: <구체적으로>
```

### 2026-09-19 — B0(프로토) 뼈대 통합 프로토타입
- 완료: 빌드 없는 단일 해시 라우팅 프로토타입. 영업·솔루션·유지보수 목록 · 프로젝트 상세 ·
  계약 상세 · 이슈 2종 · 홈(위젯 커스터마이즈) · 이관 파이프라인 전 구간 동작.
  스냅샷 `prototype/snapshots/001-b0-backbone`
- 검증: `prototype/pipeline-test.js` **단언 35건 통과**(브라우저 없이 node로 실행).
  이 목록이 B5 스펙의 수용기준 초안이다. verify 8단계 초록
- 발견·수정: ① 채번 시작값이 시드 id와 겹쳐 **새 프로젝트가 시드 프로젝트를 덮어썼다** —
  로직 테스트가 9건 실패로 잡았다(화면만 눌렀으면 한참 뒤에 발견). 시드 최대 id 위에서 채번하도록 수정.
  ② `snapshots frozen` 검사가 스냅샷 스크립트 자신의 INDEX 행 추가를 막았다 —
  append-only는 불변이 아니다. 행 삭제만 막도록 수정(행 주석은 나중에 채울 수 있어야 한다)
- 예산: **1,270줄 / 400 초과.** 뼈대 전체 + F6 + F8을 한 프로토타입에 담았으므로 의도된 초과다.
  예산 규칙을 `prototype/` 제외로 고치고 근거를 결정 기록에 남겼다(현재 48/400)
- 리뷰에서 바뀐 것: 이관해도 기존 영역 목록에 남기기 · 100% 전제조건 · `착수`→`진행 전` ·
  1:1 확정 + 이관 내역 파생 · 이관 내역을 상시 노출에서 **버튼 + 펼치기**로
- 미해결: BACKBONE §4에 7건 남음(계약 갱신 1:N · 영업 미확정 표현 · 이관 권한 ·
  "진행 중만" 필터 · 이관 건 수정 경로 · 솔루션 자체 생성 · 이관 시 사이트 수)
- 다음 작업: B1 스펙 (`spec/b1-backbone`)

### 2026-09-19 — 모델 구성 확정 + 기계 검사 이관
- 완료: 기본 모델 opus-5를 settings.json에 고정, reviewer에 `model: sonnet` 지정.
  reviewer의 BLOCKER 8건 중 기계 항목 6건을 verify로 이관 — `cycle discipline`(브랜치를
  의도 선언으로 삼아 스냅샷·스펙 선행 조건 검사, main 작업 차단) · `prototype build-free` ·
  `script line endings` 3단계 신설. reviewer에는 판단 5건만 남김
- 검증: 실패 실험 8건 통과 — main에서 프로토타입 변경 FAIL / spec 브랜치에 스냅샷 없음 FAIL →
  생성 후 PASS / feat 브랜치에 스펙 없음 FAIL → 생성 후 PASS / prototype/package.json FAIL /
  CRLF 주입 FAIL → 복원 PASS
- 발견·수정: `check_script_eol` 첫 구현(awk)이 **CRLF 파일에 거짓 PASS**를 냄 — Git Bash의
  awk가 CR을 삼킨다. tr 방식으로 교체 후 재실험 통과. 파이썬으로 스크립트를 패치하다
  `\r` 이스케이프가 실제 CR로 해석돼 verify.sh 문법이 깨진 것도 같은 뿌리(수정 완료)
- 미해결: 없음
- 다음 작업: B1 — 세 영역 골격 프로토타입 (`proto/b1-backbone` 브랜치)

### 2026-09-19 — 컨벤션 3종 승계(개정) + 기술 스택 확정
- 완료: v3 `docs/conventions` 3종을 v4 기준으로 개정해 이식 — java-spring(패치 16건,
  §5 모듈 목록 교체·MCP 조항 부록으로 이동) · react-ts(패치 9건, 챗 위젯 절 부록으로 이동) ·
  git-workflow(단일 트랙으로 전면 재작성). 스택 확정을 CLAUDE.md·ROADMAP에 반영,
  불변식 7번(모듈은 필요해진 사이클이 만든다) 신설
- 검증: verify에 **문서 참조 검사** 신설 — 죽은 참조 주입 시 FAIL·복원 시 PASS 확인.
  현재 문서 8종의 리포 경로 참조 전부 해소. verify 5단계 전부 초록
- 미해결: 세부 버전(Java·Boot) 미고정 — 앱을 세우는 사이클에서. 모듈 구성 미정(B5 이후)
- 다음 작업: B1 — 세 영역 골격 프로토타입 (`proto/b1-backbone` 브랜치)

### 2026-09-19 — 하네스 기본 구성 적용 + 진행 방향 재설정
- 완료: `harness_practice`의 6축(Context·Persist·Verify·Control·Action·역할분리)을 v4에
  이식. v4 고통에 맞춰 세 부품 신설 — ① 프로토타입 우선 사이클(`/proto`·`/spec` 커맨드 +
  스냅샷 스크립트), ② 스냅샷 append-only 이중 잠금(settings deny + verify 검사),
  ③ diff 400줄 예산 보고. BACKBONE·ROADMAP 신설, .gitattributes로 LF 고정(v3 승계).
- 검증: 부품별 실패 실험 통과 — 깨진 링크 FAIL·복원 PASS / Stop 훅 3상태(정상 0·깨짐 2+피드백·복원 0) /
  스냅샷 연번·잘못된 라벨 거부 / 예산 0→120→초과 WARN·문서 500줄은 미집계 / 얼린 스냅샷 수정 시 FAIL
- 추가: 사용자 지시로 제품 축을 PMS로 고정 — CLAUDE.md 서문·불변식 6번, ROADMAP F9 항으로 반영(MCP는 기능 단위)
- 미해결: git 초기 커밋 미실행(사용자 확인 대기). 기술 스택 미정
- 다음 작업: B1 — 세 영역 골격 프로토타입 (`/next`)
