# <NN-slug> — <단위 이름>

> **한 페이지를 넘기지 않는다.** 넘으면 단위가 크다는 뜻이니 쪼갠다.
> 목차가 필요해진 스펙은 이미 실패한 스펙이다.

- 상태: 초안 | 승인됨 | 구현완료
- 프로토타입 스냅샷: `prototype/snapshots/<NNN>-<label>/`
- 승인일:

## 1. 무엇을 (한 줄)

## 2. 화면 · 동작

프로토타입에서 확인된 것만 쓴다. **프로토타입에 없는 화면은 스펙에 없다.**

## 3. 데이터

이 단위가 새로 만들거나 바꾸는 테이블·컬럼만. 전체 ERD를 다시 그리지 않는다.

## 4. 규칙 (불변식)

지켜야 하는 것을 must / never로 쓴다. "~이다"가 아니라 "~여야 한다"로.

## 5. 이 단위가 하지 않는 것  ← 필수

다음 단위로 넘기는 것을 명시한다. 여기 적은 것은 `docs/ROADMAP.md`에도 올린다.

## 6. 완료 조건

- [ ] `bash scripts/verify.sh` 통과
- [ ] 
